function rv = latticevector(v)
% function rv = latticevector(v)
% convert real space lattice vector to reciprocal and vice versa.
% input [
% returns in angstrom and 1/angs.
% see drawunitcell, powder2d

c23 = cross(a2, a3);
c31 = cross(a3, a1);
c12 = cross(a1, a2);
b1 = 2*pi*c23/dot(a1, c23);
b2 = 2*pi*c31/dot(a2, c31);
b3 = 2*pi*c12/dot(a3, c12);
a = [a1;a2;a3];b = [b1;b2;b3];